name: Promote Packages
description:
  Promote published package versions to the npm `latest` dist-tag using GitHub
  Actions OIDC (trusted publishing). Must run from release.yml.
inputs:
  DRY_RUN:
    description: Specify whether the action should run without publishing
runs:
  using: 'docker'
  image: 'Dockerfile'
